# Emoji assets

These SVGs are [OpenMoji](https://openmoji.org) line drawings (black variant,
v15.0.0), used by the `emoji-sketch` component. They are licensed
**CC BY-SA 4.0**, not MIT — see
[`/licenses/OpenMoji-CC-BY-SA-4.0.txt`](../../../licenses/OpenMoji-CC-BY-SA-4.0.txt)
for the full attribution and share-alike notice before reusing or modifying
them.

Filenames are Unicode code points (e.g. `1F600.svg` = 😀), matching the
OpenMoji CDN paths that `emoji-sketch.tsx` falls back to at runtime.
